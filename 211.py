#the range() function
#the loop through a set of code specified no of times we can use
# the range() function
# the range () return the sequence of numbers starts to 0 to

for x in range(6):
    print(x)

    # note that the range (6) is npt the value of 0 to 6
    # but the values 0 to 5
