#split string
#the split method return a list where the text between
#the specified separator become a list itselfe
# the split method split a string to substring
#if it find instances of the separator
a = "HELLO,WORLD"
print(a.split(","))